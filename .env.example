# Database
POSTGRES_USER=soulledger
POSTGRES_PASSWORD=changeme123
REDIS_PASSWORD=changeme456

# Django
DJANGO_SECRET_KEY=your-super-secret-key-here-change-in-production
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com

# API
DATABASE_URL=postgres://soulledger:changeme123@postgres:5432/soulledger
REDIS_URL=redis://:changeme456@redis:6379/0

# Encryption — REQUIRED when DEBUG is off.
# Without it, death-sync webhook secrets and death registration payloads are
# stored UNENCRYPTED; settings refuses to start when DEBUG=False and this is
# empty, and warns when DEBUG=True. Generate one with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# Sentry (optional)
SENTRY_DSN=

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
